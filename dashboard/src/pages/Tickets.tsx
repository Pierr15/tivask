import {useState} from 'react';import {Search,ArrowUpRight,Send,Ticket as TicketIcon,MessageSquare,User,RefreshCw} from 'lucide-react';import {useResource} from '../hooks/useResource';import {api,send,type Ticket,type Message} from '../services/api';import {Badge,ErrorBox,Empty,Loading,PageHead,date} from '../components/UI';
export function TicketsPage(){
 const [status,setStatus]=useState('');const [priority,setPriority]=useState('');const [search,setSearch]=useState('');const r=useResource<Ticket[]>('/tickets?status='+status+'&priority='+priority,10000);
 const [selected,setSelected]=useState<{ticket:Ticket;messages:Message[]}|null>(null);const [text,setText]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const [notice,setNotice]=useState('');
 async function open(id:string){try{setSelected(await api('/tickets/'+id));setNotice('');}catch(e){setError((e as Error).message);}}
 async function update(patch:Record<string,string>){if(!selected)return;try{await send('/tickets/'+selected.ticket.id,patch,'PATCH');await open(selected.ticket.id);await r.refresh();}catch(e){setError((e as Error).message);}}
 const rows=(r.data??[]).filter(t=>(t.id+t.question+t.whatsappNumber).toLowerCase().includes(search.toLowerCase()));
 return <><PageHead eyebrow="HUMAN SUPPORT" title="Saatnya sentuhan manusia." description="Tindak lanjuti pertanyaan khusus dan balas langsung melalui WhatsApp yang terhubung."><button className="btn" onClick={()=>void r.refresh()}><RefreshCw size={16}/>Refresh</button></PageHead><ErrorBox message={error||r.error}/><div className={'ticket-layout '+(selected?'has-detail':'')}><section className="panel ticket-list"><div className="toolbar"><div className="search-field"><Search size={17}/><input placeholder="Cari tiket…" aria-label="Cari tiket" value={search} onChange={e=>setSearch(e.target.value)}/></div><select aria-label="Filter status tiket" value={status} onChange={e=>setStatus(e.target.value)}><option value="">Semua status</option>{['OPEN','IN_PROGRESS','RESOLVED'].map(s=><option key={s}>{s}</option>)}</select><select aria-label="Filter prioritas" value={priority} onChange={e=>setPriority(e.target.value)}><option value="">Semua prioritas</option>{['LOW','NORMAL','HIGH','URGENT'].map(s=><option key={s}>{s}</option>)}</select></div>{r.loading?<Loading/>:rows.length?<div className="ticket-cards">{rows.map(t=><button key={t.id} className={'ticket-card '+(selected?.ticket.id===t.id?'selected':'')} onClick={()=>void open(t.id)}><div><span className="ticket-number"><TicketIcon size={14}/>{t.id}</span><Badge value={t.status}/></div><h3>{t.question}</h3><p><User size={13}/>{t.name||t.whatsappNumber}<span>·</span>{t.priority}</p><small>{date(t.createdAt)}<ArrowUpRight size={15}/></small></button>)}</div>:<Empty title="Tidak ada tiket">Pertanyaan yang membutuhkan panitia akan muncul di sini.</Empty>}</section>{selected?<section className="panel ticket-detail"><div className="panel-head"><div><div className="eyebrow">{selected.ticket.id}</div><h2>{selected.ticket.name||selected.ticket.whatsappNumber}</h2></div><button className="icon-btn" aria-label="Refresh percakapan" onClick={()=>void open(selected.ticket.id)}><RefreshCw size={17}/></button></div><div className="ticket-controls"><label>Status<select value={selected.ticket.status} onChange={e=>void update({status:e.target.value})}>{['OPEN','IN_PROGRESS','RESOLVED'].map(s=><option key={s}>{s}</option>)}</select></label><label>Prioritas<select value={selected.ticket.priority} onChange={e=>void update({priority:e.target.value})}>{['LOW','NORMAL','HIGH','URGENT'].map(s=><option key={s}>{s}</option>)}</select></label></div><div className="ticket-summary"><strong>Ringkasan · {selected.ticket.summarySource==='LOCAL'?'kutipan pertanyaan':'AI'}</strong><p>{selected.ticket.summary}</p><small>Alasan: {selected.ticket.reason}</small></div><div className="ticket-messages">{selected.messages.map(m=><div key={m.id} className={'conversation-message '+(m.role==='USER'?'incoming':'outgoing')}><span className="message-author">{m.role==='USER'?'Pengguna':m.role==='ADMIN'?'Panitia':'TIVAsk'}</span><p>{m.content}</p><small>{new Date(m.createdAt).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})} · {m.delivery}</small></div>)}</div><form className="reply-form" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');setNotice('');try{const result=await send<{delivery:string}>('/tickets/'+selected.ticket.id+'/reply',{message:text,requestId:crypto.randomUUID()});setText('');await open(selected.ticket.id);setNotice(result.delivery==='SIMULATED'?'Balasan simulator tersimpan. Tidak ada pesan WhatsApp yang dikirim.':'Balasan terkirim melalui WhatsApp.');await r.refresh();}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}>{notice&&<div className="success-box">{notice}</div>}<textarea aria-label="Balasan admin" placeholder="Tulis balasan panitia…" value={text} onChange={e=>setText(e.target.value)} required maxLength={4000} disabled={selected.ticket.status==='RESOLVED'}/><div><span className="muted tiny">{selected.ticket.whatsappNumber.startsWith('sim:')?'SIMULATOR · Tidak mengirim WhatsApp':'Balasan dikirim ke pengguna tiket ini'}</span><button className="btn primary" disabled={busy||!text.trim()||selected.ticket.status==='RESOLVED'}><Send size={16}/>{busy?'Mengirim…':'Kirim balasan'}</button></div></form></section>:<section className="panel ticket-placeholder"><MessageSquare size={40}/><h2>Pilih percakapan</h2><p>Buka tiket untuk membaca riwayat dan membantu pengguna.</p></section>}</div></>;
}

